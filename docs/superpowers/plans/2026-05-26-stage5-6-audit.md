# Stage 5 + Stage 6 코드 감사 + 디버깅 가능성 개선 Plan

**작성일:** 2026-05-26
**목적:** Stage 5 (Mandate Validator) + Stage 6 (Portfolio Manager) 의 4 차원 감사.
**선행:** Stage 1-4 audit 모두 머지됨 (e41982f). 22 commits 누적, staleness propagation chain 완성.

**통합 audit**: Stage 5+6 가 비교적 작아서 한 plan 으로 묶음 (mandate_validator 175 + 4 mandate skills + portfolio_manager 137 = ~500 LOC).

---

## 0. Stage 5+6 특성

- **Stage 5: LLM 호출 0회** (4 mandate hard check, deterministic)
- **Stage 6: deep_llm 호출 1회** (philosophy.md 작성)
- 두 stage 모두 **이전 attribution chain 의 종착점**:
  - Stage 5 가 attribution 안 받음 → `validation_passed=False` 시 root cause trace 어려움
  - Stage 6 의 portfolio.json full_trace 에 `allocation_attribution` / `risk_judge_attribution` **저장 안 됨**
- **mandate_validator logger 0개** — Stage 1-4 audit 의 마지막 가시화 누락 영역

---

## 1. 4 차원

| 차원 | 무엇을 본다 |
|---|---|
| **L** | mandate_validator 의 retry routing 의미, 4 check 의 tolerance 1e-6 의 정밀도 / philosophy retry logic 1회 fixed (section 누락 미검증) / qty=0 fallback silent |
| **H** | 4 mandate check 의 magic (0.20/0.25/0.70/0.80/0.10/1e-6) module-level named const / philosophy 4000 chars retry threshold / asterisk 정리 로직 |
| **D** | allocation_attribution + risk_judge_attribution → portfolio.json + philosophy state_summary. 현재 활용 0 → Stage 6 narrative 가 Stage 3+4 진단 정보 못 봄 |
| **O** | mandate_validator entry/violations log / portfolio_manager 각 output 산출 log / philosophy retry 발동 log |

---

## 2. Task 순서

1. **Task 0 — mandate_validator observability + attribution thread**: entry log, 4 check 결과 요약 logger, validation_report → attribution 노출. **`validation_passed=False` 시 어느 check 가 어느 ticker 로 fail 했는지 즉시 보임**.
2. **Task 1 — 4 mandate check named const**: 각 check 의 module-level const (HARD_SINGLE_CAP=0.20, HARD_CLUSTER_CAP=0.25, HARD_RISK_ASSET_CAP=0.70, TURNOVER_INITIAL=0.80, TURNOVER_MONTHLY=0.10, FLOAT_TOLERANCE=1e-6).
3. **Task 2 — portfolio_manager attribution thread**: `allocation_attribution` + `risk_judge_attribution` → portfolio.json full_trace 의 신규 key. philosophy state_summary 에도 가시화.
4. **Task 3 — portfolio_manager named const + logger 보강**: PHILOSOPHY_MIN_CHARS=4000, MAX_RETRIES=1 named const. 각 output 산출 시점 logger.info. trade_plan qty=0 / philosophy retry 발동 더 명시.
5. **Task 4 — 통합 sanity + audit summary**: 신규 test + 회귀 + audit doc.

---

## 3. Task 0 — mandate_validator observability + attribution thread

**Files:**
- `tradingagents/agents/validator/mandate_validator.py`

**Steps:**
- [ ] **0.1 — O** entry log: weight_vector position 수, attempts, rebalance_mode.
- [ ] **0.2 — O** 각 check 호출 후 logger.info: violation 수 (severity 별).
- [ ] **0.3 — O** 최종 결과 logger.info: validation_passed + hard violation 수 + first 3 violation 요약.
- [ ] **0.4 — D** ValidationReport 에 mandate_validator_attribution 별도 dict 안 만들고, state output 으로 `mandate_validator_attribution` 추가:
  - input_present (weight_vector / universe / clusters / capital / previous)
  - rebalance_mode
  - check_counts: {universe, concentration, correlation, turnover} → violation count
  - hard_violations: [{check, severity, ticker, description}, …] top-3
- [ ] **0.5 — E** commit: `audit(stage5): mandate_validator observability + attribution [Task0]`.

**합격 기준:** validation 실패 시 logger 한 줄로 어느 check / ticker / value 였는지 즉시 보임.

---

## 4. Task 1 — 4 mandate check named const

**Files:**
- `tradingagents/skills/mandate/concentration_check.py`
- `tradingagents/skills/mandate/correlation_check.py`
- `tradingagents/skills/mandate/turnover_check.py`
- `tradingagents/skills/mandate/universe_check.py`
- `tradingagents/agents/validator/mandate_validator.py` (FLOOR_BY_MODE)

**Steps:**
- [ ] **1.1 — H** concentration_check: HARD_SINGLE_CAP=0.20, HARD_RISK_ASSET_CAP=0.70, FLOAT_TOLERANCE=1e-6.
- [ ] **1.2 — H** correlation_check: DEFAULT_CLUSTER_CAP=0.25, FLOAT_TOLERANCE=1e-6.
- [ ] **1.3 — H** turnover_check: FLOAT_TOLERANCE=1e-9 (tighter), 의미 docstring 보강.
- [ ] **1.4 — H** universe_check: 이미 명확. docstring 보강만.
- [ ] **1.5 — H** mandate_validator: FLOOR_BY_MODE 의 TURNOVER_FLOOR_INITIAL=0.80, TURNOVER_FLOOR_MONTHLY=0.10 named const.
- [ ] **1.6 — E** commit: `audit(stage5): 4 mandate checks named const + tolerance [Task1]`.

**합격 기준:** mandate threshold 4 곳에서 분산되지 않고 import 1줄로 확인.

---

## 5. Task 2 — portfolio_manager attribution thread

**Files:**
- `tradingagents/agents/managers/portfolio_manager.py`

**Steps:**
- [ ] **2.1 — D** `_build_full_trace_portfolio` 에 신규 key 추가:
  - `allocation_attribution` (Stage 3)
  - `risk_judge_attribution` (Stage 4)
  - `mandate_validator_attribution` (Stage 5 Task 0 산출)
  - 각각 `_serialize_for_json` 적용
- [ ] **2.2 — D** philosophy.md state_summary 에 신규 텍스트:
  - "Allocator: method=X, cap_clipped=[T1, T2], cov_excluded=…"
  - "Risk Overlay: lens=tail_risk → critical, strength=0.7, final_level=2"
  - "Mandate: validation_passed=True, no violations" 또는 hard violations 요약
- [ ] **2.3 — E** commit: `audit(stage6): portfolio_manager — thread Stage 3+4+5 attribution to portfolio.json + philosophy [Task2]`.

**합격 기준:** portfolio.json 한 파일만 봐도 Stage 1-5 의 모든 작업 trace 가능.

---

## 6. Task 3 — portfolio_manager named const + logger 보강

**Files:**
- `tradingagents/agents/managers/portfolio_manager.py`

**Steps:**
- [ ] **3.1 — H** PHILOSOPHY_MIN_CHARS=4000, PHILOSOPHY_MAX_RETRIES=1, ARTIFACT_DIR_FMT="artifacts/%Y-%m-%d" named const.
- [ ] **3.2 — O** 각 output 산출 시점 logger.info:
  - "portfolio.json 산출 (n_positions=N)"
  - "trade_plan.csv 산출 (qty=0: M tickers)"
  - "philosophy.md 산출 (chars=L, retry=K)"
- [ ] **3.3 — L** philosophy retry 발동 시 logger.warning ("philosophy <4000 chars → retry").
- [ ] **3.4 — L** trade_plan qty=0 → 이미 logger.warning 있음 (line 124). state["warnings"] 에 명시적 사유 (PRICE_FETCH_FAILED vs PRICE_ZERO).
- [ ] **3.5 — E** commit: `audit(stage6): portfolio_manager — named const + per-output logger [Task3]`.

**합격 기준:** 3 output 산출이 logger 만 봐도 trace 가능. retry / qty=0 cause 명시.

---

## 7. Task 4 — Stage 5+6 통합 sanity + summary

**Files:**
- 신규/확장 test: `tests/unit/agents/test_mandate_validator.py`, `test_portfolio_manager_full_trace.py`

**Steps:**
- [ ] **4.1** 신규 test 3개:
  - `test_mandate_validator_attribution_records_violations`: Stage 5 attribution dict 가 check_counts / hard_violations 모두 채움.
  - `test_portfolio_json_includes_stage345_attribution`: portfolio.json full_trace 에 allocation/risk_judge/mandate_validator attribution 모두 포함.
  - `test_mandate_named_const_present`: 4 check + mandate_validator 의 const 존재.
- [ ] **4.2** 전체 unit + integration regression. pre-existing 1 fail 제외 회귀 0.
- [ ] **4.3** `docs/stage5_6_audit.md` 작성.
- [ ] **4.4** commit: `audit(stage5+6): integration sanity + summary [Task4]`.

**합격 기준:** Stage 1-6 audit 전체 통합 회귀 0. 신규 test 통과.

---

## 8. Sign-off Checklist

- [ ] Task 0 — mandate_validator observability + attribution
- [ ] Task 1 — 4 mandate check named const
- [ ] Task 2 — portfolio_manager attribution thread
- [ ] Task 3 — portfolio_manager named const + logger
- [ ] Task 4 — integration sanity + summary

---

## 9. 범위 밖

- mandate threshold (0.20/0.25/0.70) 값 자체 변경 → 대회 mandate 변경 시
- philosophy.md 6 section schema 변경 → 별도 작업 (Stage 6 정리 ⑥ 일부)
- trade_plan.csv 컬럼 추가 (예: 위탁수수료) → MTS 사양 변경 시
- previous_portfolio 자동 import → 별도 작업

---

## 10. Risk

| Risk | Mitigation |
|---|---|
| portfolio.json schema 변경이 외부 consumer 깨뜨림 | attribution 키 추가만 (기존 키 제거 없음). |
| state output 키 (`mandate_validator_attribution`) 충돌 | Stage 3 / 4 와 다른 이름 사용 (3 stage 각자 별도 키). |
| philosophy retry 변경이 LLM 비용 증가 | retry 횟수 유지 (1회), threshold 만 named. |
